- int a, b;
   a = b = 3; // 안될것같은 다중대입연산 가능
- printf("%d\n", -5 - -5); // 이런 식으로 띄워서 음수의 뺄셈 가능!!
- char month[] = "hellow"; // 초기화와 동시에 진행하면 사이즈지정 필요없음
- scanf(" %f",&cost);     // 아하! 이렇게 띄워서 버퍼 비워주는게 좋구나!
- scanf("%hu/%hu/%hu", &year, &month, &date);  // 강제로 뭔가 입력하게 할 수 있다